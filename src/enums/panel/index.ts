export enum PanelStateNetworkModeEnum {
  // 自动内外网模式
  'auto' = 0,
  // 外网模式
  'wan' = 1,
  // 内网模式
  'lan' = 2,
  // 编辑模式（需要密码，全部内容）
  'edit' = 3,
}

export enum PanelPanelConfigStyleEnum {
  'icon' = 1, // 图标风格
  'info' = 0, // 详情风格
  'small' = 1, // 同icon
}
